.venv/
__pycache__/
*.pyc

# Raw and prepared datasets are reproducible from scripts/configs — not committed.
data/raw/
data/interim/
data/aurum/
data/realworld/
data/batches/
data/*.db

# Training runs are large; curated outputs are copied into models/ and reports/.
runs/

# Model weights are build artifacts, not source. Rebuild via TRAINING.md.
models/*.pt

.DS_Store
node_modules/
dist/
.pytest_cache/
