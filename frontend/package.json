{
  "name": "aurum-dashboard",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Browser view of the Aurum batch ledger. Reads the FastAPI service; computes nothing of its own.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.0.4",
    "vite": "^7.1.14"
  }
}
