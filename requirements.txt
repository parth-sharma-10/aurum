# Aurum Vision — runtime dependencies.
# Pinned to the versions used for the v0.1 training run.
# Python 3.12 (PyTorch has no stable wheels for 3.14 at time of writing).

# --- model + inference -----------------------------------------------------
ultralytics==8.4.120
torch==2.13.0
torchvision==0.28.0        # required by ultralytics; pinned for reproducibility
opencv-python==5.0.0.93
numpy==2.5.2
pillow==12.3.0

# --- dataset ingest / normalization ---------------------------------------
pyyaml==6.0.3
imagehash==4.3.2           # perceptual hashing for near-duplicate leakage checks

# --- object tracking -------------------------------------------------------
# Required by Ultralytics' ByteTrack (app/vision/tracker.py) for the linear
# assignment step. Ultralytics AutoUpdate will pip-install this on first use if
# it is missing, which is a network call in the middle of a demo -- pin it here
# so it is installed up front instead.
lap==0.5.13

# --- serving + dashboard backend ------------------------------------------
fastapi==0.141.1
uvicorn[standard]==0.52.3
python-multipart==0.0.32   # required by FastAPI to accept file uploads

# --- reporting / figures ---------------------------------------------------
matplotlib==3.11.1

# --- optional --------------------------------------------------------------
pyserial>=3.5              # the HX711 and both servos, over one USB link.
#                          # Without it the dashboard uses a clearly labelled
#                          # SIMULATED sensor; app/weight.py raises a message
#                          # telling you to install it if you ask for hx711.
